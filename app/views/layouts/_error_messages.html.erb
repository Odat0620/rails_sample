<%# エラー時はエラーメッセージ作成 %>
<% if model.errors.any? %>
<div>
   <% model.errors.full_messages.each do |message| %>
   <div class="alert alert-danger">
   <%= message %>
   </div>
   <% end %>
</div>
<% end %>