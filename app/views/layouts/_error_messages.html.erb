<%# エラー時はエラーメッセージ作成 %>
<% if model.errors.any? %>
<div>
 <ul>
   <% model.errors.full_messages.each do |message| %>
   <li class='error-message error'><%= message %></li>
   <% end %>
 </ul>
</div>
<% end %>