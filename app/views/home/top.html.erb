<div class="top_container">
  <h1>Touringood</h1>
  <p>ツーリングのルート共有アプリです。</p>
  <% unless user_signed_in? %>
  <div class="sign_up">
    <%= link_to "新規登録", new_user_registration_path, class: 'btn btn-primary' %>
  </div>
  <% end %>
</div>

<%= link_to "投稿一覧", posts_path %>

<%= render 'users/shared/users_posts' %>